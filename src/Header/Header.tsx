import styles from "./styles.module.css";

function Header() {
  return (
    <header>
      <img src="./logo.png" alt="klekt" className={styles.logo} />
      coding test
    </header>
  );
}

export default Header;
